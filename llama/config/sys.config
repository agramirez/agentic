[{kernel,[
    {logger,[
        {handler, default, logger_std_h, #{
            config => #{file => "/workspace/llama/log/info.log"}
        }},
        {handler, debug, logger_std_h, #{
            level => debug,
            config => #{file => "/workspace/llama/log/debug.log"}
        }}
    ]}
]}].