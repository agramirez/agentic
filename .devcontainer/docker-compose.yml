version: '3'

services:
  dev:
    container_name: agentic-dev
    build: 
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ../:/workspace
      - ../data/gguf:/usr/local/share/gguf